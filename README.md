# Тестовое задание в компанию Arenadata
## Позиция "Junior Java Разработчик"
## Содержание
- [Техническое задание](https://github.com/IutkinEgor/ArenadataTest/blob/master/doc/Technical_Requirements.md)
- [Архитектура приложения](https://github.com/IutkinEgor/ArenadataTest/blob/master/doc/Architecture.md)
- Комментарий к техническому заданию
- Заключение по проделанной работе
- Дальшнейшее развитие проекта
- Установка
  
## Комментарий к техническому заданию
### Решение проблемы с API Rate limit и обработка ошибок:
1. Для обхода решения предусмотрено использование нескольких API ключей. Все ключи из переменной среды собираются в структуру данных Queue. На каждый вызов из очереди получаем ключ, производим вызов, возвращаем ключ в конец очереди. Тем самым производим ротацию.

2. Вызовом API с предустановленным временем происходит с помощью планировщика (scheduler). В случае возникновения ошибки (rate limit, timeout) планировщик снимает задачу и уходит в паузу на установленное время. По истечению времени происходит повторное назначение задачи и попытка обращения к API. Данный сценарий применяется как при вызове API coinmarketcap (ошибка на стороне data provider модуля), так и при вызове elasticsearch (ошибка на стороне persistence модуля).

### Хранение данных:
Для работы с ELK создается индекс, который имеет следующий mapping:

```json
{
  "mappings": {
    "properties": {
      "id": {
        "type": "keyword"
      },
      "name": {
        "type": "keyword"
      },
      "symbol": {
        "type": "keyword"
      },
      "quoteHistory": {
        "type": "nested",
        "properties": {
          "date": {
            "type": "date",
            "format": "yyyy-MM-dd'T'HH:mm:ss"
          },
          "price": {
            "type": "float"
          }
        }
      }
    }
  }  
}
```
### Обработка данных
Для получения данных был разработан дополнительный адаптер - API модуль на базе HttpServer из OpenJDK com.sun. Данный модуль декларирует две конечные точки, которые возвращают данные в формате application/json:

- `/cryptocurrency/average?symbol=BTC` - вычисление средней цены криптовалюты за последний час
- `/cryptocurrency/max-change` - определение криптовалюты с максимальным процентным изменением цены за последний день

Response body: 
<table>
<tr>
<td> average </td> <td> max-change </td>
</tr>
<tr>
<td>

```json
{
    "createdAt": 1707305603841,
    "data": 2360.7811904542473,
    "code": 200,
    "success": true,
    "failure": false
}
```

</td>
<td>
    
```json
{
    "createdAt": 1707305603841,
    "data": {
        "symbol": "RANDOM",
        "percentage": -12.23,
        "today": {
            "date": "2024-02-07T08:30:00",
            "price": 100
        },
        "yesterday": {
            "date": "2024-02-06T08:30:00",
            "price": 114.23
        },
        "code": 200,
        "success": true,
        "failure": false
    }
}
```
</td>
</tr>
</table>

** Для расчета средней цены достаточно иметь хотя бы одну quoteHistory запись по бумаге за последний час. <br>
** Определение криптовалюты с максимальным процентным изменением цены за последний день требует минимум две quoteHistory записи по бумаге, одна из которых датирована предыдущим днем. Если записей недостаточно или нет исторических данных за прошлый день, данная криптовалюта не учитывается при расчете. Если ни по одной из криптовалют нет записей за прошлый день, будет возвращено соответствующее сообщение.

### e2e тестирование
На данный момент тесты отсутствуют по причине времязатратности на их написание и давности размещения вакансии. Соответственно, есть опасения, что компания подберет нужного кандидата раньше, чем я успею выполнить все пункты в ТЗ.

### Реализация на исторических данных
Для получения исторических данных на площадке coinmarketcap необходимо приобрести платное средство (даже для free trial). По этой причине было решено ограничиться бесплатным API - получение текущих котировок.

### Java не ниже 17.
Проект базируется на Java 21 и использует virtual thread в HttpServer.

## Заключение по проделанной работе
🟢 Чистая масштабируемая модульная архитектура с минимальным количеством внешних зависимостей. <br>
🟢 Покрытие развернутыми комментариями.<br>
🟢 Логирование событий.<br>
🟢 Функционал в соответствии с ТЗ.<br>
🟡 Unit тесты. Покрытие Domain и Application моделей.<br>
🔴 Покрытие e2e тестами.

## Дальнейшее развитие проекта
В качестве улучшения кодовой базы я вижу следующие идеи:
1. Рассмотреть возможность перехода от самописного DI контейнера к Java CDI 2.0.
2. Сократить количество внешних зависимостей в модулях адаптеров. Например, провести исключение транзитивных зависимостей.
3. Уменьшить количество самописных Exception классов. Обобщить и перенести их в common модуль.
4. Использовать Lombok для сокращения шаблонного кода.
5. Добавить валидацию полей на аннотациях.
6. Улучшить покрытие тестами.
7. Дополнить Docker-compose файл для быстрого старта вместе с Elasticsearch.
8. Попробовать собрать проект под GraalVM.

## Установка из исходного кода 
Приреквизиты: установленная java 21, Maven

#### 1. Колнирование репозитория
 ```shell
 git clone https://github.com/IutkinEgor/ArenadataTest.git
 ```
#### 3. Навигация в корневую директорию проекта
```
cd ArenadataTest
```
#### 5. Выполнение чистой установки через Maven
```shell
mvn clean install
```
#### 4. Создание Docker образа
```shell
docker build -t arenadata-iutkin-test:latest .
```
#### 6. Создание docker сети
```shell
docker network create iutkin-test
```
#### 6. Запуск контейнера с Elasticsearch
_В рамках тестового задания scurity часть была отключена_
```shell
docker run -d --name elasticsearch --network iutkin-test -e "xpack.security.enabled=false" -e "discovery.type=single-node" --cap-add IPC_LOCK elasticsearch:8.12.0
```
#### 7. Запустить контейнер
Для запуска приложения необходимо передать один или несколько API ключей разделённых запятой в качестве переменной среды для взаимодействия с провадйером данных (coinmarketcap) 
```shell
docker run --name arenadata-iutkin-test -p 8080:8080 --network iutkin-test -e DATAPROVIDER_API_KEYS=your-key1,your-key2 arenadata-iutkin-test:latest
```
_Перечень предустановленнных системных переменных:_
```Dockerfile
# Периодичность в миллисекундах с которой приложение отправляет запрос для получения новых данных от провадйера данных (coinmarketcap)
ENV SCHEDULER_TASK_PERIOD_IN_MILLISECONDS=60000
# Период ожидания перед повторным вызовом в случае возникновения ошибки (Лимит запроса, timout при обращение к провадйеру данных, elasticsearch и т.п.)
ENV SCHEDULER_TASK_PAUSE_IN_MILLISECONDS=60000
# Доменное имя провайдера данных 
ENV DATAPROVIDER_DOMAIN=https://pro-api.coinmarketcap.com
# Путь до конечной точки API провадйера данных 
ENV DATAPROVIDER_PATH=/v1/cryptocurrency/listings/latest
# Заголовок для API ключа
ENV DATAPROVIDER_API_HEADER=X-CMC_PRO_API_KEY
# Timout запроса
ENV DATAPROVIDER_REQUEST_TIMEOUT_IN_MILLISECONDS=5000
# Адресс elk
ENV PERSISTENCE_DOMAIN=http://elasticsearch:9200
# Имя индекса в elk
ENV PERSISTENCE_INDEX_NAME=crypto
# Адресс серврева внутри контейнера
ENV API_ADDRESS=0.0.0.0
# Порт серврева внутри контейнера
ENV API_PORT=8080
```

